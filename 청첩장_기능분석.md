# 청첩장 프로젝트 기능 분석

> 모바일 청첩장 웹 사이트의 주요 기능을 정리한 문서입니다.
> 신규 기능 추가·기존 기능 개선·유지보수 시 어떤 영역이 어떤 방식으로 동작하는지 빠르게 파악할 수 있도록 구성했습니다.

최종 갱신: 2026-05-29

---

## 0. 프로젝트 개요

- **유형**: 정적 HTML + Vercel Serverless API (Vercel KV 저장소)
- **설정 일원화**: 거의 모든 표시 정보가 `setting.json` 한 곳에서 관리됨
- **모바일 우선 디자인**: max-width 480px, iOS Safari + Android Chrome 호환
- **컬러**: 골드 `#C9A96E` · 크림 `#FAF7F2` · 다크골드 `#8B6F47` · 로즈 `#B76E79`

### 페이지 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 메인 청첩장 |
| `jhkim-parent_2.html` | 신랑측 혼주용 변형 (혼주 메시지/공유 문구 차이) |
| `hjlee-parent_3.html` | 신부측 혼주용 변형 |
| `link.html` | 청첩장 공유 진입 페이지 (메인/신랑측/신부측 분기) |
| `nav.html` | 길찾기 (네이버지도/카카오맵/카카오내비/T맵) |
| `cal.html` | 일정 등록 (Google Calendar / iCal 다운로드) |
| `photo.html` | 사진 업로드 관리 페이지 |
| `result.html` | RSVP 회신 집계 관리 페이지 |

### API 엔드포인트 (`api/*.js`, Vercel Functions + KV)

| 엔드포인트 | 메서드 | 용도 | 저장 키 |
|---|---|---|---|
| `/api/rsvp` | GET/POST/DELETE | 참석 회신 응답 | `rsvp_responses` |
| `/api/photos` | GET/POST/DELETE | 갤러리 사진 (v2 키 분리 + v1 레거시 병합) | `gallery_index` + `photo:<id>` + `gallery_photos` |
| `/api/parents` | GET/PUT | 혼주 연락처 4명 | `wedding_parent_phones` |
| `/api/accounts` | GET/PUT | 송금 계좌 정보 | `wedding_accounts` |

---

## 1. 다중 청첩장 변형 (메인 / 신랑측 / 신부측)

**무엇인가**
같은 결혼식이지만 누가 보내느냐에 따라 (신랑 본인 / 신랑 부모 / 신부 부모) 인사말·서명·공유 문구가 미묘하게 다른 3종 청첩장을 별도 HTML 파일로 운영.

**왜 가치 있는가**
실제 한국 결혼 문화에서 혼주(부모)가 주변에 청첩장을 돌리는 경우가 많고, 본인이 보내는 청첩장과는 어조가 달라야 함 ("저희가 결혼합니다" vs "저희 아들이 결혼합니다"). 한 파일로 분기 처리하면 조건문이 복잡해지고 실수가 늘기 때문에, 차라리 파일을 분리해 변형마다 독립적으로 유지보수.

**구현 난이도와 트레이드오프**
- **장점**: 각 변형이 단순한 정적 HTML, 진입 URL만 다르면 됨 (`link.html` 에서 분기)
- **단점**: **세 파일 drift 위험** — 공통 기능(갤러리 슬라이더, 송금 모달 등)을 한 파일만 수정하고 나머지를 잊으면 페이지마다 동작이 달라짐
- 장기적으로는 공통 JS/CSS 를 외부 파일로 분리하는 리팩터링이 유효 (현재는 HTML 안에 인라인)

**핵심 고려사항**
세 파일 동시 수정 시 반드시 `git diff` 로 동일 패치가 들어갔는지 검토. 카카오 공유 버튼·갤러리 슬라이더처럼 자주 손대는 영역은 특히 주의.

---

## 2. 설정 일원화 (`setting.json`)

**무엇인가**
신랑신부 이름·예식일·웨딩홀·메시지·계좌·갤러리 사진·RSVP 마감 등 거의 모든 정보를 `setting.json` 단일 파일에서 관리. HTML 페이지가 `fetch('/setting.json')` 으로 읽어 DOM 에 주입.

**왜 가치 있는가**
청첩장 내용은 자주 수정됨 (사진 추가, 혼주 메시지 변경, 시간 조정 등). HTML 을 직접 편집하면 마크업과 데이터가 섞여 변경 추적이 어려운데, JSON 으로 분리하면 비개발자도 수정 가능하고 git diff 가 깔끔.

**구현 난이도와 트레이드오프**
- **장점**: 데이터-뷰 분리, 변경 비용 최소화
- **단점**: 로컬 파일 직접 열기로는 fetch CORS 차단 → 반드시 로컬 서버 (`python -m http.server`) 또는 배포 환경 필요
- 일부 동적 데이터(사진·연락처·계좌)는 추가로 KV 에서 덮어쓰기 → 두 소스 관계가 처음 보는 사람에게는 헷갈림

**핵심 고려사항**
`setting.json` 은 정적 기본값, Vercel KV 는 운영 중 수정용. 갤러리 사진이 KV 에 없으면 `setting.json` 의 `gallery.photos` 로 폴백되는 패턴 — 코드 곳곳에 같은 폴백 로직이 반복됨.

---

## 3. 사진 갤러리 (업로드 + 슬라이더)

**무엇인가**
`photo.html` 에서 사진을 업로드(base64) 하면 `/api/photos` POST 로 KV 에 저장되고, 청첩장 페이지에서 슬라이더로 자동 재생. 점 인디케이터·하단 썸네일·터치/마우스 스와이프·켄번즈 줌인·그라데이션 페이드 전환 지원.

**왜 가치 있는가**
사진은 청첩장에서 가장 자주 바뀌는 콘텐츠. HTML 을 수정하지 않고 업로드만으로 갱신할 수 있으면 비개발자가 직접 관리 가능. 슬라이더 인터랙션 품질은 청첩장 첫 인상에 큰 영향.

**구현 난이도와 트레이드오프**
- **저장 구조 v2 분리**: 사진별로 `photo:<id>` 키 + `gallery_index` 순서 배열로 저장 → 누적해도 단일 키 크기 폭주 회피 (v1 단일 배열은 KV 1MB 한계에 빠르게 도달)
- **레거시 호환**: GET 시 v2 신규 + v1 레거시(`gallery_photos`) 를 합쳐 반환 → 과거 사진 보존
- **base64 인라인 저장**: 외부 스토리지(S3 등) 불필요하지만 KV 비용↑·전송량↑ → 사진 수가 많아지면 부담

**핵심 고려사항**
- 슬라이더 코드는 3개 HTML 파일에 동일하게 복제됨 (drift 위험)
- 진단 로그가 주석으로 보존되어 있어 비슷한 버그 재발 시 빠르게 켜서 디버깅 가능 (`[diag 이력 2026-05-17]` 검색)
- 슬라이드 전환은 CSS Grid 스택 + `opacity` + `mask-image` 그라데이션으로 부드러운 페이드 (translateX 슬라이드 방식 폐기)
- 상세 구조는 `갤러리_슬라이더_구조.md` 참조

---

## 4. 송금 모달 (토스/카카오페이 딥링크 + 폴백 체인)

**무엇인가**
"마음 전하기" 카드 클릭 → 모달에서 토스/카카오페이/계좌번호 복사 중 선택. 앱 딥링크 시도 후 일정 시간 내 앱이 열리지 않으면(`visibilitychange`/`blur` 미감지) 다음 앱으로 폴백, 최종적으로 계좌번호 복사.

**왜 가치 있는가**
한국 결혼식 축의금 문화에서 송금 편의성은 실제 회수율에 직접 영향. 사용자의 폰에 어떤 송금 앱이 설치되어 있는지 알 수 없으므로 자동 폴백이 사용자 친화적.

**구현 난이도와 트레이드오프**
- **장점**: 별도 SDK 없이 URL 스킴 + 이벤트 감지만으로 구현 가능
- **단점**: 딥링크 동작이 OS/브라우저/앱 버전마다 미세하게 다름 → iOS Safari, Android Chrome, In-App Browser(카카오톡 내장 브라우저) 각각 테스트 필요
- 폴백 타임아웃(보통 1.5초~2초) 이 너무 짧으면 정상 앱 실행을 실패로 오판, 너무 길면 UX 답답함

**핵심 고려사항**
계좌번호 복사는 `navigator.clipboard.writeText` 우선, 실패 시 `document.execCommand('copy')` 폴백 — 구형 iOS Safari 호환용. 복사 성공 토스트를 반드시 표시해야 사용자가 확신.

---

## 5. 길찾기 (4종 지도 앱 분기)

**무엇인가**
오시는 길 카드에서 길찾기 버튼 클릭 → 모달에서 네이버지도/카카오맵/카카오내비/T맵 선택. 각 앱별 딥링크(`nmap://`, `kakaomap://`, `kakaonavi-sdk://`, `tmap://`) 시도 + 웹 폴백.

**왜 가치 있는가**
한국 사용자는 네이버지도와 카카오맵 사용자가 비등하고, 운전자는 카카오내비/T맵을 선호. 한 앱만 지원하면 30~50% 사용자가 불편을 겪음.

**구현 난이도와 트레이드오프**
- **장점**: Google Maps API 키 불필요 (임베드는 `output=embed`, 길찾기는 외부 앱 위임)
- **단점**: 각 앱별 URL 스킴 규격이 자주 바뀜 → 앱 업데이트마다 동작 검증 필요
- 좌표 정확도가 사용자 경험을 좌우 (특히 자동차 길찾기) → `setting.json` 의 `venue.lat/lng` 가 실제 정문 좌표와 일치하는지 주기적 확인

**핵심 고려사항**
모바일/데스크톱 분기 (`isMobile`) — 데스크톱에서는 앱 딥링크가 무의미하므로 웹 검색 URL 로 바로 이동.

---

## 6. RSVP (참석 회신 + 집계)

**무엇인가**
청첩장 하단 폼에서 이름·연락처·신랑/신부측·참석 여부·인원수 입력 → `/api/rsvp` POST 로 KV 저장. `result.html` 에서 전체 목록 조회·개별 삭제·전체 초기화 + CSV 내보내기 가능.

**왜 가치 있는가**
종이 청첩장 시대의 회신 엽서를 디지털화. 신랑신부와 웨딩홀이 실시간 인원 파악 가능 → 식대 정산·좌석 배치에 직결.

**구현 난이도와 트레이드오프**
- **장점**: 폼 + 단일 KV 키만으로 MVP 완성, 비용 거의 0
- **단점**: 인증 없음 → `result.html` URL 노출 시 누구나 응답 목록 열람·삭제 가능 (단순 obscurity 의존)
- 응답 1건 추가마다 전체 배열 재저장 → 응답 수 천 건 이상이면 성능·동시성 문제 발생 가능

**핵심 고려사항**
- 필수 검증: `name`, `side`, `attend` — 누락 시 400 반환
- 보안 강화 옵션: 관리 페이지에 간단한 PIN/토큰 게이트 (현재 없음 — 신뢰 가능한 사용자에게만 URL 공유 전제)

---

## 7. 카카오톡 공유 (Kakao Share SDK)

**무엇인가**
공유 버튼 클릭 → Kakao Share SDK 의 `sendDefault` 호출로 feed 템플릿(타이틀·설명·이미지·버튼 2개) 카드를 카카오톡 채팅창에 보냄. 카드 본문 클릭은 청첩장 URL, 버튼은 청첩장 보기 + 길찾기.

**왜 가치 있는가**
한국 결혼 청첩장 공유의 1순위 경로가 카카오톡. 일반 URL 만 전송하면 미리보기가 부실하지만 SDK 를 쓰면 풀 카드로 노출되어 클릭률이 높아짐.

**구현 난이도와 트레이드오프**
- **장점**: SDK 1줄 호출, 미리보기 이미지·버튼 커스텀 가능
- **단점**: Kakao 개발자 콘솔 등록 + 도메인 화이트리스트 필요, 카카오 정책 변경 시 미리보기 캐시 무효화 위해 이미지 URL 쿼리 버전(`?v=5`) 필요
- feed 템플릿은 버튼 최대 2개 제한 → 청첩장 보기/길찾기/일정 등록 중 선택 (현재는 청첩장 보기 + 길찾기, 일정 등록은 주석 보존)

**핵심 고려사항**
- 카드 본문 클릭으로 청첩장이 열리므로 버튼은 보조 액션에 집중
- 4개 페이지(`index/jhkim_2/hjlee_3/link`) 모두 동일 패턴으로 호출 → 변형마다 보낸 사람 이름이 다른 점만 유의

---

## 8. 혼주/계좌 정보의 동적 관리

**무엇인가**
혼주 4명 연락처와 신랑신부 계좌 정보를 `setting.json` 정적 기본값 외에 `/api/parents`, `/api/accounts` (PUT/GET) 로 덮어쓸 수 있음. 청첩장 페이지 로드 시 KV 에 값이 있으면 우선 사용.

**왜 가치 있는가**
실수로 전화번호·계좌번호가 잘못 표기된 채 배포된 경우 다시 빌드/배포하지 않고 즉시 수정 가능. 결혼식 직전 변경(혼주 변경, 계좌 변경) 도 빠르게 반영.

**구현 난이도와 트레이드오프**
- **장점**: 정적 사이트 + 소규모 동적 데이터의 좋은 절충 (전체 DB 도입 부담 없이 핵심 값만 KV)
- **단점**: 수정 UI 없음 → API 직접 호출(curl/Postman) 또는 별도 관리 페이지 필요
- 동기화 책임이 분산됨 (`setting.json` 의 값과 KV 의 값이 다르면 어느 쪽이 권위인지 명확히 해야 함)

**핵심 고려사항**
현재 권위는 KV > setting.json. 신규 배포 시 `setting.json` 만 바꾸면 KV 가 덮어쓰므로 무효한 점에 주의. 운영 정책으로 "수정은 KV PUT 으로, 초기값만 `setting.json`" 정도로 정리 필요.

---

## 9. 빠른 참조: 기능별 코드 진입점

| 기능 | 주요 파일 | 핵심 함수/식별자 |
|---|---|---|
| 갤러리 슬라이더 | `index.html` 외 2종 | `renderGallery()`, `goSlide()`, `setupSliderSwipe()`, `#sliderTrack` |
| 카카오 공유 | `index.html` 외 3종 (`link.html` 포함) | `sendKakao()`, `Kakao.Share.sendDefault` |
| 송금 모달 | `index.html` 외 2종 | 모달 컨테이너 + 토스/카카오페이 딥링크 핸들러 |
| 길찾기 | `nav.html` (단독 페이지) + 청첩장 모달 | `app === 'navermap' | kakaomap | kakaonavi | tmap'` 분기 |
| RSVP | `index.html`, `result.html`, `/api/rsvp` | 폼 submit 핸들러 + KV CRUD |
| 사진 업로드 | `photo.html`, `/api/photos` | base64 변환 + POST |
| 혼주/계좌 동적 | `/api/parents`, `/api/accounts` | KV PUT/GET (관리 UI 없음) |

---

## 10. 보완 권장 사항 (현 시점에서 식별된 갭)

1. **세 청첩장 파일 공통 JS 분리** — `js/gallery.js`, `js/kakao-share.js`, `js/transfer.js` 형태로 외부화하면 drift 사라짐
2. **관리 페이지 인증** — `result.html`, `photo.html`, 혼주·계좌 PUT 에 PIN/토큰 게이트 추가
3. **사진 외부 스토리지 마이그레이션** — base64 누적이 KV 비용에 부담되면 Vercel Blob 또는 S3 로 이전
4. **딥링크 동작 자동 검증** — 송금/길찾기 앱 URL 스킴 변경 시 알 수 있도록 분기별 수동 체크리스트 또는 CI 의 가벼운 모니터링
5. **카카오 공유 미리보기 이미지 캐시** — Kakao 캐시 무효화는 쿼리 버전(`?v=5`) 으로 처리 중 → 이미지 갱신 시 버전 올리는 절차를 README/유지보수 가이드에 명시

---

## 11. 관련 문서

- [`README.md`](README.md) — 프로젝트 소개, `setting.json` 스키마, 실행 방법
- [`갤러리_슬라이더_구조.md`](갤러리_슬라이더_구조.md) — 갤러리 슬라이더 상세 구조, 진단 로그 카탈로그, 수정 워크플로우
