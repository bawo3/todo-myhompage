# 개인 홈페이지 기능 5가지 분석

---

## 1. 대시보드 (통계 시각화)

**무엇인가**
완료율, 우선순위별 분포, 요일별 등록 패턴을 차트로 보여주는 개인 생산성 대시보드.

**왜 가치 있는가**
현재 앱은 데이터를 쌓고 있지만(`createdAt`, `priority`, `completed`) 전혀 활용하지 않는다. 이미 localStorage에 누적된 데이터를 시각화하면 추가 백엔드 없이 즉시 인사이트를 줄 수 있다.

**구현 난이도와 트레이드오프**
- Canvas API 또는 Chart.js CDN으로 순수 프론트엔드 구현 가능
- 단, 데이터가 적을 때는 차트가 의미 없어 보임 → 최소 10개 이상일 때만 표시하는 조건부 렌더링 필요
- localStorage만 쓰므로 기기 간 데이터 공유 불가 (개인 홈페이지 한계)

**핵심 고려사항**
현재 `id: Date.now()` 구조는 타임스탬프 기반이라 요일/월별 집계가 쉽다. `createdAt` 문자열을 `new Date()`로 파싱하면 바로 활용 가능.

---

## 2. 태그 / 카테고리 시스템

**무엇인가**
할 일마다 `#업무`, `#개인`, `#공부` 같은 태그를 붙이고, 태그별 필터링과 색상 구분을 제공.

**왜 가치 있는가**
현재 필터는 완료/미완료 2단계뿐이다. 실생활에서 할 일은 맥락(context)이 다르기 때문에, 같은 "미완료"라도 지금 당장 해야 할 일과 언젠가 할 일이 섞여 있으면 인지 부하가 커진다. 태그는 이 문제를 해결하는 GTD(Getting Things Done) 방법론의 핵심 개념이다.

**구현 난이도와 트레이드오프**
- 입력창에 `#태그` 파싱 로직 추가 (정규식 `/\#[\w가-힣]+/g`) → 비교적 쉬움
- 태그별 색상 자동 배정은 해시 함수로 HSL 색상 생성 가능
- 태그가 많아지면 필터 UI가 복잡해짐 → 드롭다운 또는 태그 클라우드로 설계해야 함
- 기존 데이터 마이그레이션 필요 (저장된 todo에 `tags: []` 기본값 추가)

**핵심 고려사항**
우선순위와 태그는 상호보완적이다. 우선순위가 "얼마나 급한가"를 나타낸다면, 태그는 "어떤 영역인가"를 나타낸다. 두 기준을 조합한 매트릭스 뷰(아이젠하워 매트릭스)로 확장도 가능.

---

## 3. 마감일 / 리마인더 시스템

**무엇인가**
각 할 일에 마감일(`dueDate`)을 설정하고, D-day 표시, 기한 초과 경고, 브라우저 알림(Notification API)을 제공.

**왜 가치 있는가**
현재 앱에는 시간 개념이 "등록일"밖에 없다. 마감일이 없으면 우선순위 숫자에 의존해야 하는데, 이는 주관적이고 갱신되지 않는다. 마감일은 시간이 지남에 따라 자동으로 긴박감을 표현해주는 객관적 지표다.

**구현 난이도와 트레이드오프**
- `<input type="date">` 추가로 날짜 입력은 쉬움
- D-day 계산: `Math.ceil((dueDate - today) / 86400000)`
- 브라우저 알림은 `Notification.requestPermission()` → 사용자 허가 필요, 모바일 지원 제한적
- 마감일 지난 항목을 빨간색으로 표시하는 것만으로도 UX 효과가 크다
- 정각 알림은 `setTimeout`/`setInterval`로는 탭을 닫으면 작동 안 함 → Service Worker 필요 (복잡도 급증)

**핵심 고려사항**
마감일과 우선순위가 충돌할 수 있다. 예를 들어 우선순위 1번인데 마감일이 한 달 뒤, 우선순위 10번인데 내일 마감인 경우. 정렬 로직을 "우선순위 단독" → "마감 임박도 + 우선순위 가중치 합산"으로 바꿔야 진짜 유용해진다.

---

## 4. 반복 할 일 (Recurring Tasks)

**무엇인가**
"매일 운동", "매주 월요일 회의록 작성" 처럼 주기적으로 자동 재생성되는 할 일.

**왜 가치 있는가**
개인 홈페이지의 핵심 사용 패턴은 루틴 관리다. 매번 같은 항목을 수동으로 입력하는 것은 앱의 존재 이유를 스스로 부정하는 것이다. 반복 할 일은 앱을 "매일 켜고 싶은 도구"로 만드는 핵심 기능이다.

**구현 난이도와 트레이드오프**
- 데이터 모델: `{ recurrence: 'daily' | 'weekly' | 'monthly' | null, lastGeneratedDate: '...' }`
- 앱 로드 시 오늘 날짜와 `lastGeneratedDate` 비교 → 새 인스턴스 자동 생성
- 반복 원본과 생성된 인스턴스를 어떻게 구분할지가 설계의 핵심 (ID 체계 필요)
- "완료 후 X일 뒤 재생성" vs "매X요일 재생성" — 두 방식의 차이를 사용자가 이해하기 어려움 → UI 설명 필요
- localStorage만으로 충분히 구현 가능 (백엔드 불필요)

**핵심 고려사항**
오프라인/앱 미실행 기간 처리가 가장 까다롭다. 3일 동안 앱을 안 켰다가 켰을 때 3개를 한꺼번에 생성할지, 오늘 것만 생성할지 정책을 명확히 해야 한다. 대부분의 좋은 앱은 "오늘 것만" 정책을 택한다.

---

## 5. 내보내기 / 공유 기능

**무엇인가**
할 일 목록을 CSV·JSON·텍스트로 내보내거나, 읽기 전용 URL로 공유하는 기능.

**왜 가치 있는가**
localStorage는 완전한 사일로(silo)다. 기기를 바꾸거나 브라우저를 바꾸면 데이터가 사라진다. 내보내기는 이 한계를 우회하는 가장 단순하고 안전한 방법이다. 또한 "오늘 할 일 목록"을 팀원이나 가족과 공유하고 싶은 사용 시나리오도 있다.

**구현 난이도와 트레이드오프**
- **CSV/JSON 내보내기**: `Blob` + `URL.createObjectURL`로 다운로드 → 매우 쉬움, 10줄 내외
- **텍스트 복사**: `navigator.clipboard.writeText()` → 간단하지만 HTTPS 필요
- **URL 공유**: 데이터를 Base64로 인코딩해 URL 쿼리스트링에 담기 → 데이터 많으면 URL이 수천 자가 됨, 실용적 한계 있음
- **진짜 공유**: Firebase/Supabase 같은 외부 서비스 연동 → 복잡도와 비용 급증

**핵심 고려사항**
내보내기는 동시에 **백업** 기능이다. "가져오기(import)" 기능을 함께 만들어야 쌍이 완성된다. JSON 내보내기 → JSON 가져오기 쌍을 만들면 기기 간 이전 문제를 완전히 해결할 수 있다. 이것이 외부 서버 없이 개인 홈페이지가 택할 수 있는 최선의 데이터 지속성 전략이다.

---

## 우선순위 추천 요약

| 순위 | 기능 | 이유 |
|------|------|------|
| **1** | 마감일 시스템 | 현재 앱의 가장 큰 구멍, 구현 쉬움 |
| **2** | 내보내기/가져오기 | localStorage 사일로 문제 해결, 데이터 안전성 |
| **3** | 태그 시스템 | 사용 패턴이 다양해질수록 필수 |
| **4** | 반복 할 일 | 데일리 사용 동기 부여 |
| **5** | 대시보드 | 데이터 충분히 쌓인 후 의미 있음 |
