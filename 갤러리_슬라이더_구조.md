# 갤러리 슬라이더 구조 & 진단 로그 가이드

> 청첩장 페이지 갤러리 슬라이더의 코드 구조, 진단 로그 위치, 수정 절차를 정리한 문서입니다.
> 갤러리 관련 수정·디버깅 작업 전에 이 문서를 먼저 읽으면 어디를 봐야 할지 빠르게 찾을 수 있습니다.

최종 갱신: 2026-05-18

---

## 1. 영향 받는 파일 (3개 청첩장 페이지)

세 파일은 **동일한 갤러리 슬라이더 로직**을 가지고 있으므로 한 곳을 고치면 나머지 두 곳도 같이 고쳐야 합니다 (drift 방지).

| 파일 | 설명 |
|---|---|
| `index.html` | 기본 청첩장 (메인) |
| `jhkim-parent_2.html` | 신랑측 부모 청첩장 변형 |
| `hjlee-parent_3.html` | 신부측 부모 청첩장 변형 |

> ⚠️ **공통 패턴 수정 시 체크리스트**: 한 파일을 고친 뒤 `git diff` 로 다른 두 파일에도 같은 패치가 들어갔는지 반드시 확인.

---

## 2. 데이터 흐름

```
[사용자가 photo.html 에서 사진 업로드]
            ↓
[ API: /api/photos 로 저장 ]
            ↓
[ 청첩장 페이지 로드 → renderGallery() 호출 ]
            ↓
   ┌────────────────────────┐
   │ fetch('/api/photos')   │
   └─────────┬──────────────┘
       성공 │       실패/빈 배열
            ↓            ↓
       photos[]    setting.json 의 CFG.gallery.photos 폴백
            ↓            ↓
       └────────┬────────┘
                ↓
       슬라이드/도트/썸네일 DOM 생성
                ↓
       setupSliderSwipe() + startAutoSlide()
```

---

## 3. 핵심 함수와 DOM ID

### renderGallery() (각 청첩장 파일의 약 L3130~L3230 부근)
1. `/api/photos` 호출 → 실패 시 `console.error('사진 목록 로드 실패:', e)`
2. 빈 배열이면 `CFG.gallery.photos` 폴백
3. `#gallery-section`, `#gallery-divider` 표시
4. `#sliderTrack` 에 `<div class="slide"><img …>…</div>` 배열 주입
5. `#sliderDots` 에 점 버튼 배열 주입
6. `#thumbStrip` 에 썸네일 배열 주입
7. `setupSliderSwipe()` 로 터치/마우스 드래그 바인딩
8. `goSlide(0)` + `startAutoSlide()` 시작

### 관련 DOM ID
- `#sliderTrack` — 메인 슬라이드 트랙
- `#sliderDots` — 페이지네이션 점
- `#thumbStrip` — 하단 썸네일
- `#gallery-section`, `#gallery-divider` — 섹션 자체

---

## 4. 진단 로그 카탈로그 (2026-05-17 시점)

세 파일 모두 동일하게 **주석 형태로 보존**되어 있습니다. 비슷한 증상(2번째 사진 안 보임 등) 재발 시 주석을 풀어 빠르게 진단 가능.

| # | 위치 | 용도 | 재활성화 방법 |
|---|---|---|---|
| 1 | `function showToast` 직후 | 스크립트 로드 즉시 마커 (🟢 ... loaded) | 해당 `// console.log(...)` 줄 앞 `// ` 삭제 |
| 2 | `renderGallery()` 진입 직후 | 함수 호출 여부 추적 (🔵 renderGallery() called) | 동일 |
| 3 | photos fetch 후 폴백 직전 | 각 사진 src 유효성 진단 (📸 + 길이/base64-url 구분) | forEach 블록 6줄 모두 `// ` 제거 |
| 4 | 슬라이드 `<img>` 의 `onerror` | (과거 onload/onerror 콘솔 로그) | HTML 주석으로만 이력 보존. 재활성화 시 `photos.map(p => …)` 를 `photos.map((p, i) => …)` 로 바꾸고 onerror/onload 핸들러 안에 `console.*` 추가 |

> 진단 마커 ‑ 본문에 모두 **`[diag 이력 2026-05-17]`** 헤더가 붙어 있어 grep 으로 한 번에 찾을 수 있습니다.
> `grep -rn "\[diag 이력" *.html`

---

## 5. 슬라이더 부가 기능

| 기능 | 함수 | 비고 |
|---|---|---|
| 자동 슬라이드 | `startAutoSlide()` | 일정 간격 자동 전환, 사용자 인터랙션 시 일시정지 권장 |
| 수동 이동 | `goSlide(i)` | 도트/썸네일 클릭, 스와이프 모두 이 함수 호출 |
| 스와이프 | `setupSliderSwipe()` | 터치 + 마우스 드래그, `_swipeBound` 플래그로 중복 바인딩 방지 |
| 이미지 폴백 | `onerror=` 인라인 | 로드 실패 시 그라데이션 배경 표시 (`linear-gradient(135deg,#FAF7F2,#F0EAE0)`) |

---

## 6. 수정 워크플로우

### 6.1 갤러리 동작을 바꿀 때
1. 세 파일 중 **하나**(예: `index.html`) 에서 먼저 수정 + 로컬 확인
2. 동일 패치를 나머지 두 파일에 적용
3. `git diff` 로 세 파일 변경분이 동일 패턴인지 검토
4. 커밋 메시지에 영향 받은 파일 명시 (예: "갤러리: 자동 슬라이드 간격 5초 → 7초 — index/jhkim_2/hjlee_3")

### 6.2 진단 로그를 다시 켤 때 (디버깅)
1. 영향 파일에서 `[diag 이력` 검색
2. 필요한 항목의 `// ` 만 제거 (헤더 주석은 그대로)
3. 로컬 또는 dev 배포에서 콘솔 확인
4. **반드시 다시 주석화한 뒤 push** — 운영 콘솔에 진단 로그가 계속 찍히지 않도록

### 6.3 진단 로그를 완전히 제거할 때
- 권장하지 않음. 같은 버그가 재발했을 때 처음부터 다시 작성해야 하므로 주석 상태로 두는 게 효율적.

---

## 7. 진단 → 정리 → 배포 작업 패턴 (2026-05-15 ~ 2026-05-17 사례)

비슷한 버그가 또 발생하면 동일한 흐름으로 처리:

```
[증상 보고]
   ↓
[diag: 진단 로그 커밋] ← 가능한 작은 단위로 여러 번
   ↓
[원인 파악 + 버그 수정 커밋]
   ↓
[진단 로그 주석화 (이력 보존) + 운영용 정리 커밋]
   ↓
[push → 자동 배포]
```

관련 커밋 이력:
```
e2080c0 chore: 갤러리 진단 로그 비활성화 (주석 형태로 이력 보존)
35e1706 갤러리 슬라이더 2번째 사진 안 보이던 버그 수정
d12be93 diag: 슬라이드 이미지 onload/onerror 로깅 — 2번째 사진 진단
8345c30 diag: 모든 청첩장 페이지에 JS 로드 마커 + renderGallery 진입 로그
a395cf8 diag: 갤러리 사진 src 진단 로그 추가
145abe6 갤러리 큰 사진 로드 안되던 문제 + 스와이프 제스처 추가
```

---

## 8. 알아두면 좋은 점

- **세 파일의 drift 위험**: 같은 코드를 세 곳에 복사해 두었기 때문에, 한 곳만 고치고 다른 곳을 잊으면 페이지마다 동작이 달라집니다. 장기적으로는 공통 JS 파일로 분리하는 리팩터링이 유효합니다.
- **`onerror` 인라인 핸들러는 HTML 속성 안에 있어서 JS 주석(`//`)으로 막을 수 없음** — 함수 내부 statement 만 제거하고 위 줄에 HTML 주석으로 이력을 남깁니다.
- **사진 src 가 base64 인지 URL 인지가 진단의 핵심** (1번 사진은 URL, 2번은 base64 형태로 들어와서 길이/CSP 차이로 미로드되는 패턴이 과거에 있었음).

---

## 9. 빠른 참조: 어디를 봐야 하나?

| 하고 싶은 것 | 봐야 할 곳 |
|---|---|
| 자동 슬라이드 속도 변경 | `startAutoSlide()` 의 `setInterval` 인자 |
| 폴백 배경색 변경 | `onerror="this.parentElement.style.background='...'` |
| 갤러리 자체를 끄고 싶음 | `setting.json` 의 `gallery.enabled` 를 `false` (단, API 사진이 있으면 그대로 표시됨 — 함수 초입에서 조기 return 추가 필요) |
| 사진 업로드 UI | `photo.html` |
| 사진 저장 형식 | v2 — 사진별 키로 분리 (커밋 `d204de5` 참조) |
